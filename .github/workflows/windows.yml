name: Windows

on: [push, pull_request]

jobs:
  debug:
    uses: workflows/windows-base.yml@v1
    with:
      build_type: Debug
  release:
    uses: workflows/windows-base.yml@v1
    with:
      build_type: Release
